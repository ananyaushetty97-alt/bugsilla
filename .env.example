# Required in production. Match this exactly to the public HTTPS URL.
APP_ORIGIN=https://your-bugsilla.example.com
NODE_ENV=production
PORT=3001

# Optional: enables notification delivery. Without these, notifications remain in the local outbox.
# SMTP_URL=smtp://username:password@smtp.example.com:587
# SMTP_FROM=bugsilla@example.com
